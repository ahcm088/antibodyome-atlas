% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{aab_download}
\alias{aab_download}
\title{Download a dataset's data file}
\usage{
aab_download(atlas_id, destdir = tempdir(), destfile = NULL, ref = "main")
}
\arguments{
\item{atlas_id}{An atlas id, e.g. `"AAB-000004"`.}

\item{destdir}{Directory to save the file into. Defaults to a temp dir.}

\item{destfile}{File name to save as. Defaults to the last path segment
of `dataset_link`.}

\item{ref}{Git ref (branch, tag, or commit SHA) to read from. Defaults to
`"main"` (the latest curated data). Pin to a release tag (e.g.
`"v1.0.0"`) for reproducible results in a publication.}
}
\value{
The local file path, invisibly.
}
\description{
Downloads whatever is at the record's `dataset_link`. For records with
`record_type == "DATASET"` this is usually a repository landing page (e.g.
a GEO accession page) rather than a direct file: check
`curated_availability` first, and note that some repositories require
navigating from that page to the actual supplementary files by hand.
}
\examples{
\dontrun{
aab_download("AAB-000004")
}
}
