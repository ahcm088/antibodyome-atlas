% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{aab_platforms}
\alias{aab_platforms}
\title{List all assay platforms referenced by atlas datasets}
\usage{
aab_platforms(ref = "main")
}
\arguments{
\item{ref}{Git ref (branch, tag, or commit SHA) to read from. Defaults to
`"main"` (the latest curated data). Pin to a release tag (e.g.
`"v1.0.0"`) for reproducible results in a publication.}
}
\value{
A [tibble::tibble()] with columns `platform_id`, `name`,
  `technology`. Per-study protein/feature counts live on
  the dataset record (`aab_datasets()$platforms`), not here — see
  `schema/README.md` in the atlas repo for why.
}
\description{
List all assay platforms referenced by atlas datasets
}
