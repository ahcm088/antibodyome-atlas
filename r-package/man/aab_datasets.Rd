% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{aab_datasets}
\alias{aab_datasets}
\title{List all datasets/papers in the atlas}
\usage{
aab_datasets(ref = "main")
}
\arguments{
\item{ref}{Git ref (branch, tag, or commit SHA) to read from. Defaults to
`"main"` (the latest curated data). Pin to a release tag (e.g.
`"v1.0.0"`) for reproducible results in a publication.}
}
\value{
A [tibble::tibble()], one row per record, with columns matching
  `schema/dataset.schema.json` (`atlas_id`, `record_type`, `title`,
  `organism`, `conditions`, `total_sample_n`, `platforms`, `dataset_link`,
  `curated_availability`, etc.). List-valued fields come back as
  list-columns.
}
\description{
List all datasets/papers in the atlas
}
\examples{
\dontrun{
ds <- aab_datasets()
ds[ds$record_type == "DATASET", c("atlas_id", "title")]
}
}
