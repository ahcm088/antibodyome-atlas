% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{aab_link_status}
\alias{aab_link_status}
\title{List the link-reachability check history}
\usage{
aab_link_status(ref = "main")
}
\arguments{
\item{ref}{Git ref (branch, tag, or commit SHA) to read from. Defaults to
`"main"` (the latest curated data). Pin to a release tag (e.g.
`"v1.0.0"`) for reproducible results in a publication.}
}
\value{
A [tibble::tibble()] with one row per automated check, appended
  over time by the atlas's scheduled link checker — see
  `checked_at`/`reachable` to see whether a given dataset's link has gone
  stale.
}
\description{
List the link-reachability check history
}
