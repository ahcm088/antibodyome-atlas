% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citation.R
\name{aab_citation}
\alias{aab_citation}
\title{Get the citation for a dataset, ready to paste into a manuscript}
\usage{
aab_citation(atlas_id, ref = "main")
}
\arguments{
\item{atlas_id}{An atlas id, e.g. `"AAB-000004"`.}

\item{ref}{Git ref (branch, tag, or commit SHA) to read from. Defaults to
`"main"` (the latest curated data). Pin to a release tag (e.g.
`"v1.0.0"`) for reproducible results in a publication.}
}
\value{
A one-row [tibble::tibble()] with columns `atlas_id`,
  `dataset_link`, `citation`, `doi`, `pubmed_link`, `publisher_link`. Any
  publication field is `NA` if the record has no associated publication.
}
\description{
Get the citation for a dataset, ready to paste into a manuscript
}
\examples{
\dontrun{
aab_citation("AAB-000004")
}
}
