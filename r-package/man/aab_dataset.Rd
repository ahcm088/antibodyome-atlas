% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{aab_dataset}
\alias{aab_dataset}
\title{Get a single dataset record, with its publication and platforms resolved}
\usage{
aab_dataset(atlas_id, ref = "main")
}
\arguments{
\item{atlas_id}{An atlas id, e.g. `"AAB-000004"`.}

\item{ref}{Git ref (branch, tag, or commit SHA) to read from. Defaults to
`"main"` (the latest curated data). Pin to a release tag (e.g.
`"v1.0.0"`) for reproducible results in a publication.}
}
\value{
A list with the dataset's fields plus two resolved fields:
  `publication` (a one-row tibble, or `NULL` if none) and
  `platforms_detail` (a tibble joining `platforms[].platform_id` from the
  dataset onto `aab_platforms()`, keeping `n_proteins_reported`).
}
\description{
Get a single dataset record, with its publication and platforms resolved
}
\examples{
\dontrun{
aab_dataset("AAB-000004")
}
}
